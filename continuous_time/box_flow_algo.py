import numpy as np
import sympy as sp
from scipy.spatial import Rectangle

from velocity_field import VelocityField

def sample_box_flow_algo(initial_region : Rectangle, sample_volume : float, vf : VelocityField, dt : float,):
    pass